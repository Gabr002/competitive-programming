#include <bits/stdc++.h>
using namespace std;

int main(){
    int size, exp;

    while(cin >> size){

        for(int i = 0; i < size; i++){

            cin >> exp;
            (exp % 2 == 0) ? '1\n' : '9\n';

        }
    }
}